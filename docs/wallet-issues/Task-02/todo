fix fetch swap / transfer history data services:
+ add hard limit of 7 days for fetching, allow further data if cached
+ update the cache meta data schema for transactions - include available data range
+ update the service fetch / use cache data condition, update the function for more flexible data fetching

fix historical pnl calculation service:
+ use hard limit of 7days, use transactions in the 7days only
+ calculate using the formula: pnl(i) = change(i) - NetInflow(i) for day i 
+ change = balance at the start of day i+1 (current balance if i is today) - balance at the start of day i
+ NetInflow: Sum of transfer to the wallet - Sum of transfer out of wallet in day i (check if counterparty is a known CEX?)
