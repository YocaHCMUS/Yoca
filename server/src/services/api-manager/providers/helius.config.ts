// Helius provider config
export const heliusConfig = {
    rateLimit: 1, // requests per second
    // Env var name that holds one or more comma-separated API keys.
    apiKeyEnvVar: 'HELIUS_API_KEY',
    endpoints: {
        // Define endpoints here
    },
    errorFormat: {
        // Static error format for Helius
    },
};
